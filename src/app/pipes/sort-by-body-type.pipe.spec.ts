import { SortByBodyTypePipe } from './sort-by-body-type.pipe';

describe('SortByBodyTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByBodyTypePipe();
    expect(pipe).toBeTruthy();
  });
});
